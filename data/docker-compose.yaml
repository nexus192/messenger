version: '3.9'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: mess_user
      POSTGRES_PASSWORD: lalala
      POSTGRES_DB: mess
    ports:
      - "5432:5432"
    volumes:
      - ./storage.sql:/docker-entrypoint-initdb.d/storage.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5
